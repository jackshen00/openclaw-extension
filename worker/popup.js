// Placeholder for popup interactions
document.addEventListener('DOMContentLoaded', () => {
  // We can get worker status from storage or background
  document.getElementById('worker-id').textContent = "Worker: Local-XHS-01";
});
